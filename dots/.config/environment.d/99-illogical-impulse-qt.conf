# Qt apps and xdg-desktop-portal-kde (Discord/Electron file picker, etc.) read the
# systemd user environment at login — not Hyprland's env.lua overrides.
QT_QPA_PLATFORMTHEME=kde
QT_QPA_PLATFORM=wayland;xcb
